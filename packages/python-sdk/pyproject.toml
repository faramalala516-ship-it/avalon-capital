[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "avalon-agent-sdk"
version = "0.1.0"
description = "Python SDK for Avalon Agentique Platform agents"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["avalon_agent_sdk*"]
