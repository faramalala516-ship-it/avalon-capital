# Privilege Broker

Typed requests only: RestartAvalonService, InstallApprovedPackage, OpenApplication.

There is no `execute_powershell(string)` / `execute_cmd(string)` / `execute_admin_command(string)`.
