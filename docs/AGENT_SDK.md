# Agent SDK (Python)

Package: `packages/python-sdk` (`avalon_agent_sdk`).

APIs: register_agent, publish_event, subscribe, request_tool, request_permission, get_workspace, get_dataset, get_secret_reference, call_model, report_health, create_artifact.

Example: `agents/templates/hello-avalon`.
